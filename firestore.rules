rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Permite leitura e escrita para todos durante o desenvolvimento inicial.
      // ISSO DEVE SER ALTERADO para regras seguras antes de ir para produção.
      allow read, write: if true;
    }
  }
}
