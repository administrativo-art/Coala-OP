"use client";

// This component is no longer used. The new workflow creates a session
// with a default name immediately and redirects to a workspace.
// This modal is now obsolete and can be removed.
export function StartPurchaseSessionModal() {
  return null;
}
