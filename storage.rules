rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Permite a leitura pública de todos os arquivos.
      allow read;
      // Permite a escrita apenas para usuários autenticados.
      allow write: if request.auth != null;
    }
  }
}
